{
  "name": "fraudchain-hardhat",
  "private": true,
  "devDependencies": {
    "hardhat": "2.22.6",
    "@nomicfoundation/hardhat-ethers": "3.0.5",
    "ethers": "6.11.1",
    "dotenv": "16.4.5"
  },
  "scripts": {
    "compile": "hardhat compile",
    "deploy:local": "hardhat run ./scripts/deploy.js --network localhost"
  }
}
