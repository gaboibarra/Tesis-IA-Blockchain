# fraudchain — E2E Fraud Detection → Flask → Solidity → Ganache (Windows-only)
Python **3.13-aware**. On-chain **sin PII** (solo \decision_id\, \	xRefHash\).
Esta estructura se generó en el **Paso 1**.
